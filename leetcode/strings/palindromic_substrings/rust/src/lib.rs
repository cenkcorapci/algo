u can// Template Rust implementation for the Palindromic Substrings problem.
// The function below is a stub that returns 0. Replace with a full
// implementation when you're ready to solve the problem.

/// Count palindromic substrings in `s`.
/// TODO: implement the algorithm. This stub returns 0.
pub fn count_substrings(s: &str) -> i32 {
    // Placeholder implementation; does not allocate additional memory.
    0
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn empty_string() {
        assert_eq!(count_substrings(""), 0);
    }
}

